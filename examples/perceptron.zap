var input = [[1,0,1]
            ,[1,1,0]
            ,[0,1,0]
            ,[0,0,0]]

var output =[1,1,0,0]
var weights = [.35,.45,.11]

var tout

??var count = [500];count > [0]; count = count - [1];
!count;
tout = input.weights

![Tout is]
!tout

//error = tout - output

//weights = weights+input.(error*tout)

//![Weights are]
//!weights

